# ========== DATABASE ==========
# MongoDB Connection String
# Local Development (Docker):
# DATABASE_URL=mongodb://payload:payload@localhost:27017/nextjs_tailwind_daisyui

# Production (MongoDB Atlas):
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/nextjs_tailwind_daisyui?retryWrites=true&w=majority

# ========== PAYLOAD CMS ==========
# Payload CMS Secret Key (use a strong random string)
# PAYLOAD_SECRET=argue-crispy-change-in-production

# ========== DEPLOYMENT ==========
# Server URL (for production, update to your actual domain)
# NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# ========== STORAGE (Optional) ==========
# Vercel Blob Storage (optional - only needed if using Vercel Blob for media storage)
# BLOB_READ_WRITE_TOKEN=vercel_blob_rw_

# ========== DEMO MODE (Optional) ==========
# Forces app to use only fallback data (no CMS fetching)
# Useful for testing, demos, or builds without database access
# DEMO_MODE=true
